apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend-posts-ingress
  namespace: production
spec:
  tls:
    - secretName: frontend-production-posts-com-tls
      hosts:
        - blog-posts.com
  rules:
  - host: blog-posts.com
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: frontend-internal-service
            port: 
              number: 80
  ingressClassName: nginx

---

apiVersion: networking.k8s.io/v1
kind: IngressClass
metadata: 
  name: nginx
  namespace: production
spec:
  controller: k8s.io/ingress-nginx