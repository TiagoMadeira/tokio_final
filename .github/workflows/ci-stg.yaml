name: CI

on:
  workflow_dispatch:
  pull_request:
    branches: [development]
  push:
    branches:
      - development

jobs:
  test-auth-service:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: test auth service
        uses:  ./.github/actions/test_service
        with:
          jwt_secret: ${{secrets.JWT_KEY}}
          service_path: "auth_service"

  test-post-service:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: test post service
        uses:  ./.github/actions/test_service
        with:
          service_path: "post_service"

  test-rest-service:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: test rest service
        uses:  ./.github/actions/test_service
        with:
          service_path: "rest_service"

  trivy-scan-auth-service-stg:
     runs-on: ubuntu-latest
     environment: staging
     steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Trivi Scan
        uses: ./.github/actions/trivy_scan
        with:
          requirements: ${{ vars.REQUIREMENTS_FILE }}
          service_path: "auth_service"

  trivy-scan-post-service-stg:
     runs-on: ubuntu-latest
     environment: staging
     steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Trivi Scan
        uses: ./.github/actions/trivy_scan
        with:
          requirements: ${{ vars.REQUIREMENTS_FILE }}
          service_path: "post_service"
  
  trivy-scan-rest-service-stg:
     runs-on: ubuntu-latest
     environment: staging
     steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Trivi Scan
        uses: ./.github/actions/trivy_scan
        with:
          requirements: ${{ vars.REQUIREMENTS_FILE }}
          service_path: "rest_service"

  build-push-auth-service-stg:
    runs-on: ubuntu-latest
    needs: [ test-auth-service, test-post-service, test-rest-service, trivy-scan-auth-service-dev, trivy-scan-post-service-dev, trivy-scan-rest-service-dev ]
    environment: staging
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build push dev
        uses: ./.github/actions/build-push
        with:
          context_path: "auth_service"
          docker-file-path: ${{vars.AUTH_SERVICE_DOCKERFILE}}
          dockerhub-usernane: ${{vars.DOCKER_USERNAME}}
          dockerhub-access-token: ${{secrets.DOCKERHUB_ACCESS_TOKEN}}
          dockerhub-repository: ${{vars.AUTH_SERVICE_DOCKERHUB_REPOSITORY}}

  build-push-post-service-stg:
    runs-on: ubuntu-latest
    needs: [ test-auth-service, test-post-service, test-rest-service, trivy-scan-auth-service-stg, trivy-scan-post-service-stg, trivy-scan-rest-service-stg ]
    environment: staging
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: ./.github/actions/build-push
        with:
          context_path: "post_service"
          docker-file-path: ${{vars.POST_SERVICE_DOCKERFILE}}
          dockerhub-usernane: ${{vars.DOCKER_USERNAME}}
          dockerhub-access-token: ${{secrets.DOCKERHUB_ACCESS_TOKEN}}
          dockerhub-repository: ${{vars.POST_SERVICE_DOCKERHUB_REPOSITORY}}

  build-push-rest-service-stg:
    runs-on: ubuntu-latest
    needs: [ test-auth-service, test-post-service, test-rest-service, trivy-scan-auth-service-stg, trivy-scan-post-service-stg, trivy-scan-rest-service-stg ]
    environment: staging
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Login to Docker Hub
        uses: ./.github/actions/build-push
        with:
          context_path: "rest_service"
          docker-file-path: ${{vars.REST_SERVICE_DOCKERFILE}}
          dockerhub-usernane: ${{vars.DOCKER_USERNAME}}
          dockerhub-access-token: ${{secrets.DOCKERHUB_ACCESS_TOKEN}}
          dockerhub-repository: ${{vars.REST_SERVICE_DOCKERHUB_REPOSITORY}}