scan:
  scanners:
    - vuln
    - secret

  skip-files:
    - "./requirements.txt"
  
  file-patterns:
    - "pip:requirements-dev.txt" 

severity:
  - HIGH
  - CRITICAL

vulnerability:
  ignore-unfixed: true